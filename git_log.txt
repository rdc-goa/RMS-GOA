commit fb28f2c3a7ce522a82fb884117edba230beef5c5
Author: Pranav Rathi <pranavrathi07@gmail.com>
Date:   Mon Mar 30 13:20:18 2026 +0530

    check1

diff --git a/src/app/api/get-staff-name/route.ts b/src/app/api/get-staff-name/route.ts
index 4094305..a9a14a9 100644
--- a/src/app/api/get-staff-name/route.ts
+++ b/src/app/api/get-staff-name/route.ts
@@ -1,37 +1,22 @@
 import { NextRequest, NextResponse } from 'next/server';
-import * as XLSX from 'xlsx';
-import path from 'path';
-import fs from 'fs';
+import { getFacultyByEmail } from '@/services/umsService';
 
 export async function POST(request: NextRequest) {
   try {
     const { email } = await request.json();
-    if (!email) {
+    if (!email || typeof email !== 'string') {
       return NextResponse.json({ success: false, error: 'Email is required' }, { status: 400 });
     }
 
-    const filePath = path.resolve(process.cwd(), 'goastaffdata.xlsx');
-    if (!fs.existsSync(filePath)) {
-      return NextResponse.json({ success: false, error: 'goastaffdata.xlsx not found' }, { status: 500 });
-    }
-
-    const workbook = XLSX.readFile(filePath);
-    const sheetName = workbook.SheetNames[0];
-    const worksheet = workbook.Sheets[sheetName];
-    const jsonData: Array<Record<string, any>> = XLSX.utils.sheet_to_json(worksheet);
+    const profile = await getFacultyByEmail(email);
 
-    // Find staff by email (case-insensitive)
-    const staff = jsonData.find((row) => {
-      return row['Email'] && row['Email'].toLowerCase() === email.toLowerCase();
-    });
-
-    if (staff && staff['Name']) {
-      return NextResponse.json({ success: true, name: staff['Name'] });
-    } else {
+    if (!profile) {
       return NextResponse.json({ success: true, name: null });
     }
-  } catch (error: unknown) {
-    const message = error instanceof Error ? error.message : 'Internal Server Error';
+
+    return NextResponse.json({ success: true, name: profile.name || null });
+  } catch (error: any) {
+    const message = error?.message || 'Internal Server Error';
     return NextResponse.json({ success: false, error: message }, { status: 500 });
   }
 }

commit 879f9d3f466e426a0c61e1be7d4a08058c1703ca
Author: EDC-PU <87274086+prathi912@users.noreply.github.com>
Date:   Mon Mar 9 16:15:58 2026 +0530

    NEW PORTAL

diff --git a/src/app/api/get-staff-name/route.ts b/src/app/api/get-staff-name/route.ts
index b2c6639..4094305 100644
--- a/src/app/api/get-staff-name/route.ts
+++ b/src/app/api/get-staff-name/route.ts
@@ -10,10 +10,9 @@ export async function POST(request: NextRequest) {
       return NextResponse.json({ success: false, error: 'Email is required' }, { status: 400 });
     }
 
-    // Read staffdata.xlsx from the project root
-    const filePath = path.resolve(process.cwd(), 'staffdata.xlsx');
+    const filePath = path.resolve(process.cwd(), 'goastaffdata.xlsx');
     if (!fs.existsSync(filePath)) {
-      return NextResponse.json({ success: false, error: 'staffdata.xlsx not found' }, { status: 500 });
+      return NextResponse.json({ success: false, error: 'goastaffdata.xlsx not found' }, { status: 500 });
     }
 
     const workbook = XLSX.readFile(filePath);

commit ec7cc8193574754879ab241da9cd47a669f6ca19
Author: prathi912 <pranavrathi07@gmail.com>
Date:   Thu Jul 24 14:22:49 2025 +0530

    a

diff --git a/src/app/api/get-staff-name/route.ts b/src/app/api/get-staff-name/route.ts
new file mode 100644
index 0000000..b2c6639
--- /dev/null
+++ b/src/app/api/get-staff-name/route.ts
@@ -0,0 +1,38 @@
+import { NextRequest, NextResponse } from 'next/server';
+import * as XLSX from 'xlsx';
+import path from 'path';
+import fs from 'fs';
+
+export async function POST(request: NextRequest) {
+  try {
+    const { email } = await request.json();
+    if (!email) {
+      return NextResponse.json({ success: false, error: 'Email is required' }, { status: 400 });
+    }
+
+    // Read staffdata.xlsx from the project root
+    const filePath = path.resolve(process.cwd(), 'staffdata.xlsx');
+    if (!fs.existsSync(filePath)) {
+      return NextResponse.json({ success: false, error: 'staffdata.xlsx not found' }, { status: 500 });
+    }
+
+    const workbook = XLSX.readFile(filePath);
+    const sheetName = workbook.SheetNames[0];
+    const worksheet = workbook.Sheets[sheetName];
+    const jsonData: Array<Record<string, any>> = XLSX.utils.sheet_to_json(worksheet);
+
+    // Find staff by email (case-insensitive)
+    const staff = jsonData.find((row) => {
+      return row['Email'] && row['Email'].toLowerCase() === email.toLowerCase();
+    });
+
+    if (staff && staff['Name']) {
+      return NextResponse.json({ success: true, name: staff['Name'] });
+    } else {
+      return NextResponse.json({ success: true, name: null });
+    }
+  } catch (error: unknown) {
+    const message = error instanceof Error ? error.message : 'Internal Server Error';
+    return NextResponse.json({ success: false, error: message }, { status: 500 });
+  }
+}
